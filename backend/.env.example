# Veritabanı Bağlantısı
# Aşağıdaki seçeneklerden birini kullanın ve # işaretini kaldırın

# SEÇENEK 1: SQL Server Authentication (Kullanıcı Adı/Şifre)
# DATABASE_URL="sqlserver://localhost:1433;database=UniversiteBilgiSistemi;user=sa;password=YOUR_PASSWORD;encrypt=true;trustServerCertificate=true;schema=dbo"

# SEÇENEK 2: Windows Authentication (Önerilen - Güvenli)
DATABASE_URL="sqlserver://YOUR_SERVER_NAME;database=UniversiteBilgiSistemi;integratedSecurity=true;encrypt=true;trustServerCertificate=true;schema=dbo"

# SEÇENEK 3: SQL Server Express (Yerel Kurulum)
# DATABASE_URL="sqlserver://localhost\\SQLEXPRESS;database=UniversiteBilgiSistemi;integratedSecurity=true;encrypt=true;trustServerCertificate=true;schema=dbo"

# Örnek Server Names:
# - RAMAZANLEGION (Windows Auth)
# - localhost\SQLEXPRESS (SQL Express)
# - 192.168.1.100:1433 (Remote Server)

# JWT Ayarları
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="24h"

# Sunucu Ayarları
PORT=5000
NODE_ENV=development

# KURULUM TALİMATLARI:
# 1. Bu dosyayı .env olarak kopyalayın: cp .env.example .env
# 2. YOUR_SERVER_NAME kısmını kendi SQL Server adınızla değiştirin
# 3. Veritabanı adını kontrol edin (varsayılan: UniversiteBilgiSistemi)
# 4. JWT_SECRET'ı güvenli bir değerle değiştirin
# 5. Windows Authentication kullanıyorsanız integratedSecurity=true kullanın
# 6. SQL Authentication kullanıyorsanız user ve password ekleyin
